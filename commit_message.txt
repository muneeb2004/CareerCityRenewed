refactor: remove CV Check feature

This commit removes the "CV Check" feature from the application. The following changes have been made:
- The link to the "CV Check" page has been removed from the volunteer portal.
- The "CV Check" page file (`app/volunteer/cv-check/page.tsx`) has been deleted.
- The `cv-check` action has been removed from the `VolunteerLog` interface in `src/lib/types/index.ts`.
# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to your hosting platform's environment variables settings
# DO NOT commit actual values to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# JWT secret for signing tokens (min 32 characters, use crypto.randomBytes(32).toString('hex'))
JWT_SECRET=

# -----------------------------------------------------------------------------
# Encryption
# -----------------------------------------------------------------------------
# AES-256 encryption key for sensitive data (64 hex characters = 32 bytes)
ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# CSRF Protection
# -----------------------------------------------------------------------------
# CSRF token signing secret (min 32 characters)
CSRF_SECRET=

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# MongoDB connection string (use your enterprise cluster)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/careercity?retryWrites=true&w=majority

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
# Environment mode
NODE_ENV=production

# Public URL of your application (used for CORS, redirects, etc.)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# Security (Optional)
# -----------------------------------------------------------------------------
# Enable strict URL validation (blocks all non-whitelisted domains)
URL_VALIDATOR_STRICT_MODE=true

# -----------------------------------------------------------------------------
# Monitoring (Stage 3)
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking (get from sentry.io dashboard)
NEXT_PUBLIC_SENTRY_DSN=

# Optional: For source map uploads (better stack traces)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# =============================================================================
# GENERATED SECRETS (for reference - generate fresh ones for production)
# =============================================================================
# Run this command to generate new secrets:
# node -e "const c=require('crypto'); console.log('JWT_SECRET=' + c.randomBytes(32).toString('hex')); console.log('ENCRYPTION_KEY=' + c.randomBytes(32).toString('hex')); console.log('CSRF_SECRET=' + c.randomBytes(32).toString('hex'));"
#
# Example output (DO NOT USE THESE - generate your own):
# JWT_SECRET=1b2e621be56684b5f73314ff1c4d37ac14cddf14f34d6a382989e47b749be51c
# ENCRYPTION_KEY=3cfe0d1f620dd621fa381d55946f3edaf10782313d76ff67b89aa78a152755a6
# CSRF_SECRET=977d091531344337b1cae51401634a3e2fd84f5e5c1e944919ecbd6f23a5fbda
# =============================================================================
